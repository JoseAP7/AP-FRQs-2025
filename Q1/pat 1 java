public int walkDogs(int hour){
int numdogs = company.newAvalableDogs(hour);
if(maxDogs > numDogs){
company.updateDogs(hour,numDogs);
return numDogs;
}
companu.updatesDogs(hour,maxDogs);
return maxDogs;
}
